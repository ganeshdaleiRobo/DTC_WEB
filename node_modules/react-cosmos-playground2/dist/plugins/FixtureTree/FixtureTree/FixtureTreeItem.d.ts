import { RefObject } from 'react';
import { FixtureId } from 'react-cosmos-shared2/renderer';
declare type Props = {
    parents: string[];
    item: FixtureId;
    itemName: string;
    selectedFixtureId: null | FixtureId;
    selectedRef: RefObject<HTMLElement>;
    onSelect: (path: FixtureId) => unknown;
};
export declare function FixtureTreeItem({ parents, item, itemName, selectedFixtureId, selectedRef, onSelect, }: Props): JSX.Element;
export {};
