import { RefObject } from 'react';
import { FixtureNode } from 'react-cosmos-shared2/fixtureTree';
import { FixtureId } from 'react-cosmos-shared2/renderer';
declare type Props = {
    node: FixtureNode;
    parents: string[];
    isExpanded: boolean;
    selectedFixtureId: null | FixtureId;
    selectedRef: RefObject<HTMLElement>;
    onToggle: () => unknown;
};
export declare function FixtureTreeDir({ node, parents, isExpanded, selectedFixtureId, selectedRef, onToggle, }: Props): JSX.Element;
export {};
