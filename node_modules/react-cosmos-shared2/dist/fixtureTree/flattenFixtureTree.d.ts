import { FixtureId } from '../renderer';
import { TreeNode } from './shared/types';
export declare type FlatFixtureTreeItem = {
    fileName: string;
    fixtureId: FixtureId;
    name: string | null;
    parents: string[];
};
export declare type FlatFixtureTree = FlatFixtureTreeItem[];
export declare function flattenFixtureTree(treeNode: TreeNode<FixtureId>, parents?: string[]): FlatFixtureTree;
