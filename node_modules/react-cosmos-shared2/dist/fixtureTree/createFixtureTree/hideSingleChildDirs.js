"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.hideSingleChildDirs = hideSingleChildDirs;

function hideSingleChildDirs(rootNode) {
  var items = rootNode.items,
      dirs = rootNode.dirs;
  var dirNames = Object.keys(dirs);

  if (containsSingleDir(rootNode)) {
    return hideSingleChildDirs(dirs[dirNames[0]]);
  }

  return {
    items: items,
    dirs: dirs
  };
}

function containsSingleDir(_ref) {
  var items = _ref.items,
      dirs = _ref.dirs;
  return Object.keys(items).length == 0 && Object.keys(dirs).length == 1;
}